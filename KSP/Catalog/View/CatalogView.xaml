<UserControl x:Class="KSP.Catalog.View.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="clr-namespace:KSP.UI"
             xmlns:dialogs="http://prismlibrary.com/"
             mc:Ignorable="d" MinWidth="800" MinHeight="450"
             d:DesignHeight="450" d:DesignWidth="800">
    <dialogs:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="dialogs:Dialog.WindowStartupLocation" Value="CenterOwner" />
            <Setter Property="ResizeMode" Value="CanResizeWithGrip" />
            <!--<Setter Property="Height" Value="780"/>-->
            <Setter Property="MinHeight" Value="400"/>
            <!--<Setter Property="Width" Value="900"/>-->
            <Setter Property="MinWidth" Value="400"/>
            <Setter Property="SizeToContent" Value="Width" />
        </Style>
    </dialogs:Dialog.WindowStyle>
    <Grid>

    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
        <ui:GridControl Margin="10,0" />
    <ui:NovigatorControl Grid.Row="1" />
  </Grid>
</UserControl>